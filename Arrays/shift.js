// Pushing in the Array: (- first)

function func() {

    // Original array
    var array = ["GFG", "Geeks", "for", "Geeks"];
    console.log(array);

    // Checking for condition in array,
    var value = array.shift();
    console.log(value);
}

func();
